/**
 * Foundation: SQL schema definitions (Drizzle schema or Prisma models live nearby).
 *
 * IMPLEMENT after ORM ADR:
 * - Start empty or with a tiny `health_checks` / meta table if useful.
 * - Product tables (users, sessions, …) belong to Module 1+, not Foundation.
 */

// TODO(foundation): export schema tables once ORM is chosen
export {};
