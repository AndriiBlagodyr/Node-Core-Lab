/**
 * Foundation: database seed scripts.
 *
 * IMPLEMENT:
 * - Idempotent seeds for local/dev (and a separate path for tests later).
 * - Keep seeds out of production deploy by default.
 */

export async function seed(): Promise<void> {
  throw new Error("TODO(foundation): implement infrastructure/db/seed.ts");
}
