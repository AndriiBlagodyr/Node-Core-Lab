# Fastify API — Backend Roadmap → Foundation
# Copy to apps/api/.env and adjust. Validated by src/config/env.ts at startup.

NODE_ENV=development
HOST=127.0.0.1
PORT=8100
LOG_LEVEL=info

# Docker Compose defaults (see docker-compose.yml at repo root)
DATABASE_URL=postgresql://node_core:node_core@127.0.0.1:5432/node_core_lab
REDIS_URL=redis://127.0.0.1:6379
SMTP_URL=smtp://127.0.0.1:1025
