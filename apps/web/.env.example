# Switch the API client between mock adapters (in-process, no backend) and
# the real Fastify backend you'll implement. Default is "mock" so the app
# is fully usable from day one of the BE roadmap.
NEXT_PUBLIC_API_MODE=mock

# Used when NEXT_PUBLIC_API_MODE=real
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
